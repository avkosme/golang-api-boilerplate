---
ENVIRONMENT:
  MODE_DEV: True
  CHAT_ID:
  API_KEY:
  BOT_KEY:
  BOT_BIND_ADDRESS:
  BOT_ADDRESS:
  BOT_ID:
  BOT_PORT:
  BOT_CERT_PATH:
  BOT_KEY_PATH:
  LOG_PATH: /go/app-test.log
  REDIS_ADDRESS: redis:6379
  MONGO_ADDRESS: mongo-test
  MONGO_INITDB_ROOT_USERNAME: root
  MONGO_INITDB_ROOT_PASSWORD: root
  MONGO_DATABASE: database_test
